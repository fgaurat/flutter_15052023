import 'package:flutter/material.dart';

class ChangeNameButton extends StatelessWidget {
  const ChangeNameButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("ChangeNameButton");
  }
}
